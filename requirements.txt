# Runtime + development dependencies.
# The project itself installs with `pip install -e .[xgboost,plots,api,dev]`;
# this file exists so a plain `pip install -r requirements.txt` also works.

# Core modelling
pandas>=2.0
numpy>=1.24
scikit-learn>=1.6
joblib>=1.3
xgboost>=2.0

# Reports and notebook
matplotlib>=3.7
jupyter

# Web API and UI
fastapi>=0.110
uvicorn[standard]>=0.27

# Tests and linting
pytest>=8.0
httpx>=0.27
ruff>=0.6
